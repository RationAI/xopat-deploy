# xopat-deploy
xOpat is zero-API dependency viewer. This repository contains standalone deployment orchestration used, for example, for local runs and jupyter ntoebooks. 

> This is NOT a recommended way of running xOpat viewer. This repository only orchestrates standalone usecases of the viewer deployment - it hardwires a certain
> server usage and a certain configuration, suitable in standalone (desktop) or isolated (jupyter hub) environments.
